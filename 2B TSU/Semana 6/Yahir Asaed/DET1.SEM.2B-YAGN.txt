/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package ejerciciosingricardo53;

/**
 *
 * @author yahir
 */
//Error #3: Le faltaba un import de .File, este es importante ya que
//es el import qué genera el archivo
import java.io.File;
import java.io.FileWriter;
import java.io.IOException;

public class EjerciciosIngRicardo53 {

    public static void main(String[] args) {

        String[] productos = {"Mouse","Teclado","Monitor","USB","Laptop"};
        double[] precios = {200,350,3200,150,15000};

        double total = 0;

        try {
 //Error #1; le falta un punto y coma, esto es importante porqué sino el programa 
 //no inicia 
            FileWriter writer = new FileWriter("inventario.txt");
//Error #5: Estaba mal puesto, tenía el símbolo de menor que, cuando era mayor 
            for (int i = 0; i < productos.length; i++) {

                writer.write("Producto: " + productos[i] + "\n");
                writer.write("Precio: " + precios[i] + "\n");

                total =+ precios[i];

                //Error #1; le falta un punto y coma, esto es importante porqué sino el programa 
 //no inicia 
                if (precios[i] > 1000)
                    writer.write("Producto caro\n");

                writer.write("\n");
                        

            }
//Error #4: Le faltaba un cierre de paréntesos (), estaba mal la sintaxis
            writer.close();

            System.out.println("Inventario guardado");

        } catch (IOException e) {

            System.out.println("Error al crear archivo");

        }

    }
}
