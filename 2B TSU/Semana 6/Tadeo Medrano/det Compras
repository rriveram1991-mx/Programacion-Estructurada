 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package det;
import java.io.FileWriter;
import java.io.IOException;

/**
 *
 * @author paola
 */
public class DET {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic 
          String[] productos = {"Mouse","Teclado","Monitor","USB","Laptop"};
        double[] precios = {200,350,3200,150,15000};

       

        try {
//estaba incompleto el comando faltando ;
            FileWriter writer = new FileWriter("inventario.txt");
             double total = 0;
//al usar el igual truena el rpograma se quito el =
            for (int i = 0; i < productos.length; i++) {
                writer.write("Producto: " + productos[i] + "\n");
                writer.write("Precio: " + precios[i] + "\n");
//el contador tiene problemas solo sumar el precio y necesita agregar que se guarde
                total = total + precios[i];
                
                if (precios[i] > 1000)
                    //falta de puntos y coma
                    writer.write("Producto caro\n");

                writer.write("\n");

            }
           writer.write("precio total:" + total);
            //falta de ()
            writer.close();

            System.out.println("Inventario guardado");

        } catch (IOException e) {

            System.out.println("Error al crear archivo");

        }
 
    }
}
   
